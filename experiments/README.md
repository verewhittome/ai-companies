# Experiments

Add one folder per experiment run. Each folder can include:

- `brief.md` — what you're testing
- `config.json` — model, prompt overrides, constraints
- outputs — generated company artifacts

Shared runners live in `src/run-experiment.ts`.
